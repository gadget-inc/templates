name: pb-custom-remix-ssr

on:
  push:
    paths:
      - "shopify/product-bundler/product-bundler-custom-remix-ssr/**"
    branches:
      - main
  workflow_dispatch:

jobs:
  push-to-development-environment:
    runs-on: macos-latest
    steps:
      - name: Checkout the current repository
        uses: actions/checkout@v4
      - name: Push to development environment
        run: npx ggt push --app=product-bundler-custom-remix-ssr --env=development --force --allow-unknown-directory
        working-directory: shopify/product-bundler/product-bundler-custom-remix-ssr/
        env:
          GGT_TOKEN: ${{ secrets.PRODUCT_BUNDLER_CUSTOM_REMIX_SSR_DEPLOY_TOKEN }}
