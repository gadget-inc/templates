name: pq-public-remix-spa

on:
  push:
    paths:
      - "shopify/product-quiz/product-quiz-public-remix-spa/**"
    branches:
      - main
  workflow_dispatch:

jobs:
  push-to-development-environment:
    runs-on: macos-latest
    steps:
      - name: Checkout the current repository
        uses: actions/checkout@v4
      - name: Push to development environment
        run: npx ggt push --app=product-quiz-public-remix-spa --env=development --force --allow-unknown-directory
        working-directory: shopify/product-quiz/product-quiz-public-remix-spa/
        env:
          GGT_TOKEN: ${{ secrets.PRODUCT_QUIZ_P_REMIX_SPA_DEPLOY_TOKEN }}
