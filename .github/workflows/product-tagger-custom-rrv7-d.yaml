name: pt-custom-rrv7-d

on:
  push:
    paths:
      - "shopify/product-tagger/product-tagger-custom-rrv7-d/**"
    branches:
      - main
  workflow_dispatch:

jobs:
  push-to-development-environment:
    runs-on: macos-latest
    steps:
      - name: Checkout the current repository
        uses: actions/checkout@v4
      - name: Push to development environment
        run: npx ggt push --app=product-tagger-custom-rrv7-d --env=development --force --allow-unknown-directory
        working-directory: shopify/product-tagger/product-tagger-custom-rrv7-d/
        env:
          GGT_TOKEN: ${{ secrets.PRODUCT_TAGGER_CUSTOM_RRV7_D_DEPLOY_TOKEN }}
